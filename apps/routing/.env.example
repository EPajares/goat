# Add SENTRY_DSN to enable Sentry error tracking
SENTRY_DSN=

# Database configuration (use root .env or override here)
# POSTGRES_SERVER=
# POSTGRES_USER=
# POSTGRES_PASSWORD=
# POSTGRES_DB=
# POSTGRES_PORT=5432

# Street Network Configuration
# UUID of the edge layer in customer.layer table (derives table from user_data.street_network_line_{user_id})
# BASE_STREET_NETWORK=903ecdca-b717-48db-bbce-0219e41439cf

# Geofence table to clip the network region (schema.table format)
# NETWORK_REGION_TABLE=basic.geofence_active_mobility

# Cache directory for street network parquet files
# CACHE_DIR=/tmp/cache
